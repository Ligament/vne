

export enum VendingMachineStatus {
  ACTIVE = "active",
  INACTIVE = "inactive"
}

export enum SlotState {
  EMPTY = "empty",
  OCCUPIED = "occupied",
  RESERVED = "reserved"
}

export enum OrderStatus {
  PENDING = "pending",
  FULFILLED = "fulfilled",
  CANCELLED = "cancelled",
  DONE = "done"
}